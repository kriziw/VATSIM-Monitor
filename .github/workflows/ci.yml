name: CI

on:
  pull_request:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build workspace
        run: npm run build

      - name: Check workspace
        run: npm run check

      - name: Validate Docker Compose
        run: docker compose config

      - name: Build server image
        run: docker build -f apps/server/Dockerfile -t vatsim-monitor-server:ci .

      - name: Verify server image entrypoint
        run: docker run --rm --entrypoint sh vatsim-monitor-server:ci -c "test -f /app/apps/server/dist/index.js"

      - name: Build web image
        run: docker build -f apps/web/Dockerfile -t vatsim-monitor-web:ci .

      - name: Verify web image entrypoint
        run: docker run --rm --entrypoint sh vatsim-monitor-web:ci -c "test -f /app/apps/web/build/index.js"
